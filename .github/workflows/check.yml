name: Vize randevu kontrolü

on:
  schedule:
    # Her 10 dakikada bir (GitHub yoğunlukta birkaç dakika geciktirebilir).
    - cron: "*/10 * * * *"
  workflow_dispatch:
    inputs:
      test_notify:
        description: "Sadece test bildirimi gönder"
        type: boolean
        default: false
      report:
        description: "Sadece kaynak kapsam raporu çıkar (hangi ülkeler için veri var?)"
        type: boolean
        default: false

permissions:
  contents: read
  issues: write # randevu açılınca issue açıp GitHub'ın e-posta atmasını sağlar
  actions: write # zamanlanmış görevin 60 gün sonra kapanmasını önlemek için

concurrency:
  group: vize-check
  cancel-in-progress: false

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    env:
      # Randevu bildirimini repoda issue olarak aç; GitHub size e-posta atar.
      GITHUB_ISSUE_NOTIFY: "true"
      GITHUB_TOKEN: ${{ github.token }}
      SMTP_USER: ${{ secrets.SMTP_USER }}
      SMTP_PASSWORD: ${{ secrets.SMTP_PASSWORD }}
      SMTP_HOST: ${{ secrets.SMTP_HOST }}
      SMTP_PORT: ${{ secrets.SMTP_PORT }}
      MAIL_TO: ${{ secrets.MAIL_TO }}
      TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
      TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
      NTFY_TOPIC: ${{ secrets.NTFY_TOPIC }}
      NTFY_SERVER: ${{ secrets.NTFY_SERVER }}
      NTFY_TOKEN: ${{ secrets.NTFY_TOKEN }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Test bildirimi
        if: ${{ inputs.test_notify }}
        run: python checker.py --test-notify

      - name: Kapsam raporu
        if: ${{ inputs.report }}
        run: python checker.py --report

      - name: Önceki durumu yükle
        if: ${{ !inputs.test_notify && !inputs.report }}
        uses: actions/cache/restore@v4
        with:
          path: state
          key: vize-state-${{ github.run_id }}
          restore-keys: vize-state-

      - name: Randevuları kontrol et
        if: ${{ !inputs.test_notify && !inputs.report }}
        run: python checker.py

      - name: Durumu kaydet
        if: ${{ always() && !inputs.test_notify && !inputs.report }}
        uses: actions/cache/save@v4
        with:
          path: state
          key: vize-state-${{ github.run_id }}-${{ github.run_attempt }}

      - name: Zamanlanmış görevi açık tut
        if: ${{ always() && github.event_name == 'schedule' }}
        continue-on-error: true
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh api -X PUT "repos/${{ github.repository }}/actions/workflows/check.yml/enable" >/dev/null
